<%= flash[:success] %>
<%= flash[:notice] %>

<div class=show-box>
	<strong>Title:</strong>
	<%= @book.title %>
	<strong>Body:</strong>
	<%= @book.body %>
</div>
<%= link_to "Edit",edit_book_path(@book.id) %>|<%= link_to "Back",books_path %>